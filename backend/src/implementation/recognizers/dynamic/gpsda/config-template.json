{
  "label": "GPSDa",
  "name": "gpsda",
  "type": "recognizer-dynamic",
  "description": "A dynamic gesture recognizer that is rotation-, scale-, and position-invariant.",
  "properties": {},
  "settings": [
    {
      "type": "IntegerInput",
      "required": true,
      "label": "Number of sampling points",
      "name": "samplingPoints",
      "description": "The number of points of the gesture after resampling.",
      "minValue": 2,
      "maxValue": null,
      "default": 8
    },
    {
      "name": "alpha",
      "type": "FloatInput",
      "label": "Alpha",
      "description": "TODO.",
      "minValue": null,
      "maxValue": null,
      "required": true,
      "default": 0.8
    },
    {
      "name": "points",
      "type": "PointsSelector",
      "label": "Points",
      "description": "The points that are used by the recognizer.",
      "minNumber": 1,
      "maxNumber": null,
      "default": {}
    }
  ]
}